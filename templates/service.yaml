apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: capacitor
spec:
  type: {{ .Values.service.type }}
  selector:
    app: capacitor
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9000
