replicaCount: 1

clusterName: ""

image:
  repository: ghcr.io/gimlet-io/capacitor
  tag: v0.4.3
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9000

ingress:
  enabled: false
  className: nginx
  host: ""

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
